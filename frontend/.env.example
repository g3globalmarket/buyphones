# API Base URL
# In production, set this to your backend API URL
# If using Nginx proxy with /api prefix, use: https://yourdomain.com/api
# If backend is on a subdomain, use: https://api.yourdomain.com
VITE_API_URL=http://localhost:3000

# Inactivity Timeout (minutes)
# How long before user is automatically logged out due to inactivity
# Default: 30 minutes
VITE_INACTIVITY_TIMEOUT_MINUTES=30

# Inactivity Warning (minutes)
# How long before timeout to show warning to user
# Default: 5 minutes
VITE_INACTIVITY_WARNING_MINUTES=5
